name: openconnect # you probably want to 'snapcraft register <name>'
base: core18 # the base snap is the execution environment for this snap
version: '0.1' # just for humans, typically '1.2+git' or '1.3.2'
summary: Single-line elevator pitch for your amazing snap # 79 char long summary
description: |
  This is my-snap's description. You have a paragraph or two to tell the
  most important story about your snap. Keep it under 100 words though,
  we live in tweetspace and your description wants to look good in the snap
  store.

grade: devel # must be 'stable' to release into candidate/stable channels
confinement: devmode # use 'strict' once you have the right plugs and slots

parts:
  openconnect:
    build-packages:
      - build-essential
      - autotools-dev
      - automake
      - libtool
      - gettext
      - vpnc-scripts
      - libxml2-dev
      - zlib1g-dev
      - libssl-dev
      - pkg-config
    source: https://gitlab.com/openconnect/openconnect.git
    # See 'snapcraft plugins'
    plugin: make
    stage-packages:
      - libxml2
      - libicu60
      - vpnc-scripts
      - libatm1
    override-pull: |
      snapcraftctl pull
      ./autogen.sh
      ./configure

apps:
  openconnect:
    command: usr/local/sbin/openconnect --script /snap/openconnect/current/usr/share/vpnc-scripts/vpnc-script
    adapter: full
    plugs:
      - network
      - network-observe
      - network-control



